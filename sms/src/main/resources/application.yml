server:
  port: 8088
  servlet:
    context-path: /
  tomcat:
    max-http-form-post-size: 25MB
    max-swallow-size: 25MB

spring:
  application:
    name: sms
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 25MB
  codec:
    max-in-memory-size: 25MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sms_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: DHY20050312@
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: sms_jwt_secret_key_medical_care_system_2024_very_long_key
  expiration: 86400000

deepseek:
  api-key: sk-0f5ca1a535604dc69f44700181315489
  base-url: https://api.deepseek.com

algorithm:
  python:
    command: python
    script: app_fall_detection.py
    work-dir: ..
    base-url: http://localhost:5000
    startup-timeout-seconds: 180
    stop-on-shutdown: true
